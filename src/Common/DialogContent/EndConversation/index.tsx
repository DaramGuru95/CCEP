import { Box } from "@mui/system";
import React from "react";

type Props = {};

const EndConversation = (props: Props) => {
  return <Box>Do you want to end conversation?</Box>;
};

export default EndConversation;
